import React from 'react';
import {View, Text} from 'react-native';

export default function Results({navigation}) {
    return (
        <View>
            <Text>Results</Text>
        </View>
    );
};
